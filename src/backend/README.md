# Backend

Jiaxin owns authentication, API wiring, migrations and transactions. Chuying owns processing, matching, catalogue and analytics integration. Split is proposed for team coordination.

Read ../../docs/handoff/DATA_API_CONTRACT.md before implementation. Implement app/ and dependency locks here; use one API process and bounded child processing. Tests live in ../../tests/backend/. No real secrets/resumes in source or fixtures.
