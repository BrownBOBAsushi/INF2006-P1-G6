APP_ENV=development
APP_ORIGIN=http://localhost:8080
GOOGLE_CLIENT_ID=REPLACE_WITH_GOOGLE_WEB_CLIENT_ID
# Generate a unique value locally, for example: python -c "import secrets; print(secrets.token_hex(32))"
APP_SIGNING_KEY=GENERATE_A_UNIQUE_64_HEX_CHARACTER_SECRET
POSTGRES_USER=app_user
POSTGRES_PASSWORD=change_me_locally
POSTGRES_DB=internship_matcher
DATABASE_URL=postgresql+psycopg://app_user:change_me_locally@db:5432/internship_matcher
