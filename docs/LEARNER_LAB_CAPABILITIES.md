# Learner Lab capability evidence

**Checked:** 2026-09-26. This records planning constraints and console observations; no resources were created. The instruction text supplied for this check says it was last updated 2025-06-24, so verify limits again before provisioning.

| Area | Evidence and status | Planning consequence |
|---|---|---|
| Regions | **Supplied lab instructions** (2025-06-24): `us-east-1` and `us-west-2`. | Keep the design in these regions. |
| EC2 | **Supplied lab instructions:** On-Demand only; nano through large; at most 9 concurrently running instances and 32 vCPU per region. The example allowing 32 micro instances conflicts with the 9-instance cap; use the stricter cap. | Count instances across services. EC2 instances may be stopped at session end and restarted next session; public IPv4 can change unless an Elastic IP is associated. |
| EBS | **Supplied lab instructions:** up to 100 GB; gp2, gp3, sc1, or standard; provisioned IOPS unsupported. | Keep each planned volume within the size and type limits. |
| RDS | **Supplied lab instructions:** PostgreSQL supported; burstable nano, micro, small, or medium; On-Demand; gp2 up to 100 GB; Enhanced Monitoring unsupported. Stopped DBs can restart automatically after seven days, and lab session end may leave RDS running. | Disable Enhanced Monitoring and track retained DBs and cost. |
| RDS console | **Live console, 2026-09-26:** PostgreSQL 16.9-R2, 16.10-R2, 16.11-R1, 16.12-R1, 16.13-R2, 16.14-R2, and 16.15-R1 appeared in the engine list. A temporary Single-AZ form offered burstable `db.t3.micro` (2 vCPU, 1 GiB), `db.t3.small` (2 GiB), `db.t3.medium` (4 GiB) and corresponding `t4g` classes; gp2 minimum was 20 GiB. | Candidate for a future compatibility test: a listed PostgreSQL 16 minor, `db.t3.micro`, 20 GiB gp2, Single-AZ. Exact minor selection remains TBD. Larger options were also visible; visibility does not establish lab permission. The form was canceled and the final database inventory showed zero databases. |
| IAM | **Supplied lab instructions:** IAM is extremely limited; custom users, groups, and roles are prohibited except service-linked roles. **Live console, 2026-09-26:** `LabRole` and `LabInstanceProfile` were present, with ECR read-only, SSM managed-instance, and three lab policies attached. Policy inspection reported an explicit denial for `iam:GetPolicy`; `access-analyzer:ListPolicyGenerations` was denied because no identity-based allow was present. | Use supplied roles; do not design around custom per-component roles. No account IDs, usernames, or full ARNs are recorded here. |
| Services | **Supplied lab instructions:** S3, SQS, SNS, CloudWatch, ECR, VPC, SSM, CloudFormation, Secrets Manager, KMS, ELB, and Auto Scaling are listed for lab use. Fargate is also supported. | EC2 familiarity is a team choice, not a lab requirement. Service listing does not prove every operation or configuration will be permitted. |
| Budget and persistence | **Supplied lab instructions:** budget figures may lag actual use by 8–12 hours. EC2 can restart with the next session; RDS may keep running or restart after seven stopped days. | Check active resources directly; budget display is not immediate spend evidence. Do not assume the five lab accounts can pool quotas or budgets. |
| pgvector | **AWS documentation:** RDS for PostgreSQL 16 supports the `vector` extension, which must be enabled separately using `CREATE EXTENSION vector`. The exact extension version available for the listed lab minor is **untested**. [RDS extension support](https://docs.aws.amazon.com/AmazonRDS/latest/PostgreSQLReleaseNotes/postgresql-extensions.html) · [Enabling PostgreSQL extensions](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Appendix.PostgreSQL.CommonDBATasks.Extensions.html) | Verify extension creation, migrations, and a real vector query against the chosen lab engine before calling the database compatible. |
| Network paths | **AWS documentation:** private subnet internet egress uses NAT plus an internet gateway; an S3 gateway endpoint only covers S3; ECR image pulls can also require S3; SQS can use public endpoints over NAT or an interface endpoint; SSM needs an agent, permissions, and outbound HTTPS. [NAT scenario](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-scenarios.html) · [S3 endpoints](https://docs.aws.amazon.com/vpc/latest/privatelink/vpc-endpoints-s3.html) · [ECR endpoints](https://docs.aws.amazon.com/AmazonECR/latest/userguide/vpc-endpoints.html) · [SQS traffic](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-internetwork-traffic-privacy.html) · [SSM VPC setup](https://docs.aws.amazon.com/systems-manager/latest/userguide/setup-create-vpc.html) | An S3 endpoint alone does not provide worker access to ECR, SQS, or SSM. Validate the selected egress path from a worker. |
| Availability | **AWS documentation:** RDS Multi-AZ subnet-group coverage does not itself create a standby. [RDS VPC subnet groups](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_VPC.WorkingWithRDSInstanceinaVPC.html) | Planned Single-AZ remains a single database instance; do not claim database failover or high availability. |

## Runtime verification still required

No deployment or runtime compatibility test was performed. Before making implementation or capacity claims, verify: actual lab permission to create the chosen resources; PostgreSQL extension creation, migrations, and vector queries; worker image pulls including ECR's S3 path; SQS publish/consume, CloudWatch logs, and SSM access from the worker; security-group isolation and authenticated HTTPS; and teardown of retained or billable resources. The selected 50-submission workload remains untested. The architecture's latest-PDF retention choice is unchanged.

**Source note:** learner limits above summarize the locally supplied instruction attachment `Pasted text.txt` (instructions last updated 2025-06-24). Console observations are dated and specific to the check on 2026-09-26; they are not a promise of future availability.
